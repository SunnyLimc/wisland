<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="wisland.Views.Settings.DiagnosticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

        <Grid Background="Transparent">
                <ScrollViewer Background="Transparent"
                                          Padding="32,24,32,24">
                        <StackPanel Spacing="16"
                MaxWidth="600">
                                <TextBlock Text="Diagnostics"
                    Style="{StaticResource TitleTextBlockStyle}"/>
                                <TextBlock Text="Configure logging verbosity and access log files."
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <ComboBox x:Name="LogLevelSelector"
                    Header="Log Level"
                          SelectionChanged="LogLevelSelector_SelectionChanged">
                                        <ComboBoxItem Content="Trace" Tag="Trace"/>
                                        <ComboBoxItem Content="Debug" Tag="Debug"/>
                                        <ComboBoxItem Content="Info" Tag="Info"/>
                                        <ComboBoxItem Content="Warn" Tag="Warn"/>
                                        <ComboBoxItem Content="Error" Tag="Error"/>
                                </ComboBox>

                                <Button x:Name="OpenLogsFolderButton"
                    Content="Open Logs Folder"
                    Click="OpenLogsFolderButton_Click"/>
                        </StackPanel>
                </ScrollViewer>
        </Grid>
</UserControl>
